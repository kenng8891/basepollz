<section>
  {{!-- <h2>Create New Post</h2>

  <form class="new-game-form">
    <div>
      <label for="game-title">Title</label>
      <input type="text" id="game-title" name="game-title" />
    </div>
    <div>
      <label for="game-url">Link</label>
      <input id="game-url" name="game-url" />
    </div>
    <button type="submit" class="btn">Create</button>
  </form>
</section> --}}

{{#if games.length}}
<section>
  <h2>Your Games</h2>
  <ol>
    {{#each games as |game|}}
    <li>
      {{> game-info game}}
      {{!-- <a href="/dashboard/edit/{{game.id}}" class="edit-link">Edit game</a> --}}
    </li>
    {{/each}}
  </ol>
</section>
{{/if}}

<script src="/javascript/add-post.js"></script>